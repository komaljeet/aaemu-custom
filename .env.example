# Used by sqlx-cli if you adopt compile-time queries later. Not required at build
# time for this project (all queries are runtime-checked).
DATABASE_URL=mysql://root:password@127.0.0.1:3306/aaemu_game